<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Project Task" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="">
      <% if notice.present? %>
       <p class="absolute h-8 px-4 font-semibold text-gray-200 bg-green-600 rounded-md bottom-6 right-6"><%= notice %></p>
      <% elsif alert.present? %>
       <p class="absolute h-8 px-4 font-semibold text-gray-200 bg-red-600 rounded-md bottom-6 right-6"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
